#ifndef ADDITION_H
#define ADDITION_H

#include "item.h"

class Addition : public Item
{
public:
    Addition(std::string name);
    virtual void createItemObject(QMap<std::string, std::string> config) = 0;
    virtual QIcon getIcon() = 0;
    virtual std::string getName() = 0;
    
};

#endif // ADDITION_H